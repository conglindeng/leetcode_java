import leetcode.easy.IsValid_20;
import leetcode.middle.*;

import java.util.UUID;

public class Main {
    static long[][] arr;
    public static void main(String[] args) {
        System.out.println(UUID.randomUUID().toString().replace("-", ""));
        System.out.println(IsValid_20.isValid("()[]{"));
    }
}
