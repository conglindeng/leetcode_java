package algorithm;

public class FindKNum {
    public int findKth(int[] a, int n, int K) {
        // write code here
        return doFindKth(a, 0, n - 1, K);
    }

    private int doFindKth(int[] a, int begin, int end, int K) {
        int m = begin;
        int left = begin + 1, right = end;
        while (left < right) {
            //if ()
        }
        return 1;
    }
}
