package algorithm;

public class HeapSort {
    private int[] work;
    private int size;
    private int length;

    public HeapSort(int size, int length) {
        this.work = new int[length];
        this.size = 0;
        this.length = length;
    }

    public void add(int e) {
        int i = size;
        while (true) {
            int k = i >>> 1;

        }

    }
}
